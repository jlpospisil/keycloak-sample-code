enum Routes {
  HOME = '/',
  LOGIN = '/login',
  LOGOUT = '/logout',
  LOGIN_CALLBACK = '/login/callback',
  READ_ACCESS = '/read-access',
  WRITE_ACCESS = '/write-access',
}

export default Routes;
